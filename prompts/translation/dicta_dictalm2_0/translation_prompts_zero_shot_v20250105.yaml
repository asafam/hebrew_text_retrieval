document:
  prompt_prefix: ""
  prompt_template: |
    {text_key}: {text}
    {translation_key}:

query:
  prompt_prefix: ""
  prompt_template: |
    {context_key}: {context_text}
    {text_key}: {text}
    {translation_key}: